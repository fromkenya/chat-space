.chat-main
  .main-header
    .left-box
      .current-group
        %h2 group
      %ul.member-list
        Member :
        / - @group.users.each do |user|
        %li.member
          / = user.name
    = link_to "#" do
      .edit-btn Edit
  .main-chat
    %ul.messages
      %li.message
        .message-upper
          %p group
          %p 2019/03/03(Sun) 14:55:46
        %p message
      %li.message
        .message-upper
          %p group
          %p 2019/03/03(Sun) 14:55:46
        %p message
  .form
    %form
      .input-box
        %input.text-area{placeholder: "type a message", }
          %label.upload-label{for: "upload-icon"}
            %i.fa.fa-picture-o
            %input.image-file{id: "upload-icon", type: "file"}
      %input.submit-btn{type: "submit", value: "Send"}
.side-chat
  .side-upper
    .side-header
      %p
        = current_user.name
      %ul
        %li
          = link_to new_group_path do
            %i.fa.fa-edit
        %li
          = link_to edit_user_path(current_user) do
            %i.fa.fa-cog
  .groups
    - current_user.groups.each do |group|
      .group
        = link_to group_messages_path(group) do
          %p
            = group.name
          %p
            メッセージはまだありません。
